<!--miniprogram/pages/mybook/mybook.wxml-->
<view>
  <view wx:for="{{myBook}}" wx:key="{{index}}">
    {{item.title}}<button size="mini" type="primary" data-id="{{item._id}}" bind:tap="handleDelete">删除</button>
  </view>
</view>
